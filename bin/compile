#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e
set -o pipefail

BUILD_DIR=$1
CACHE_DIR=$2

ls -a
pwd
echo "-----> BUILD_DIR="$1
echo "-----> CACHE_DIR="$2

cd $1


echo "-----> Installing :)"
echo $(find . -maxdepth 1 -type f -name "*" ! -name "/PCube-API")

